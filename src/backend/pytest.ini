[pytest]
DJANGO_SETTINGS_MODULE = config.settings
testpaths =
    conversations/tests
    documents/tests
    users/tests
    tests/
python_files = test_*.py
python_classes = Test* *Tests
addopts = --reuse-db -v
